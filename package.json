{
  "name": "oneul-shim",
  "version": "1.0.0",
  "description": "오늘의 쉼 — 나를 위한 에너지 충전소 (셀프케어 웹앱)",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node tests/smoke.mjs",
    "beta": "node beta/simulate.mjs",
    "build:web": "node tools/build-ios.mjs",
    "build:ios": "node tools/build-ios.mjs",
    "ios": "npm run build:web && npx cap sync ios",
    "android": "npm run build:web && npx cap sync android"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "@capacitor/cli": "^6.1.2"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/haptics": "^6.0.2",
    "@capacitor/ios": "^6.1.2",
    "@capacitor/local-notifications": "^6.1.0"
  }
}