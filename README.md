# 오늘의 쉼 ☕ — 나를 위한 에너지 충전소

지치고 번아웃이 온 날, 매일 마음을 기록하고 → 데이터로 나를 이해하고 → 위로받으며 조금씩 회복하는 셀프케어 웹앱입니다.
설치 없이 브라우저에서 바로 열 수 있고, 홈 화면에 앱처럼 설치해 **오프라인**으로도 쓸 수 있어요.

## 화면 구성 (하단 탭)

### 💛 정서 케어
- **기분 7단계** — 우울/무기력/지침/불안/그럭저럭/괜찮음/활기참.
- **저녁 회고** — "오늘 가장 좋았던 / 힘들었던 순간" 2문항 (선택, 구조화 일기).
- **위로 문구 즐겨찾기 + 내 문구 추가** — 와닿는 말을 모으고 직접 써넣기. ("즐겨찾기만 보기")

### ✨ 오늘의 여정 (적응형 기록 경로)
한 번에 하나씩 따라가며 매끄럽게 하루를 기록하는 풀스크린 흐름. **오늘의 기록 + 습관까지 한 경로에**.
- 기분 → 에너지 → (기분이 무거우면) 숨 고르기 → 한 줄 일기 → 감정 태그 → 잘한 일 → (습관 있으면) 습관 체크 → (저녁이면) 회고 → 마무리
- **기분·시간대·습관 유무에 따라 경로가 분기**되고 질문 문구도 달라져요. 진행 바로 끝이 보여요.
- 오늘 탭은 '여정'이 기본, 상세 폼은 접어두어 **한 화면에서 시작**할 수 있어요.

### 🌬️ 호흡 사운드 (스파 세션)
4·7·8 호흡에 **따뜻한 드론 화음 + 숨처럼 열리고 닫히는 필터 노이즈 + 단계마다 싱잉볼** 울림.
삑삑이는 톤이 아니라 몰입형 사운드 세션이에요. 🌙 수면 모드와도 어울려요.

### 📔 오늘
- **기분 체크인** — 6가지 기분 중 선택하면 그에 맞는 따뜻한 말을 건네요.
- **에너지 슬라이더** — 오늘 에너지를 1~5칸으로.
- **일기** — 오늘 하루를 한 줄이라도 기록.
- **오늘 잘한 일** — 사소한 자기 칭찬 기록.
- **🎯 오늘의 습관** — 등록한 습관을 메인 화면에서 바로 보고, 동그라미를 눌러 **오늘 완료를 한 번에 체크**. 진행률·연속(🔥)에 더해 **습관 분석 인사이트 한 줄**(기분 연관·요즘 흐름·가장 꾸준한 습관)까지 보여줘요. (이름을 누르면 습관 상세로)
- 저장하면 **연속 기록(스트릭)** 을 응원해줘요.

### 🌿 쉼
- **나를 위한 한마디** — 응원 문구 랜덤.
- **호흡 쉼표** — 4·7·8 호흡 애니메이션 + 부드러운 저음 가이드 사운드. 매 초 카운트.
- **🌙 수면 모드** (빠른 호흡 ▶ 토글) — 화면을 어둡게, 카운트음 끄고 잔잔한 패드+빗소리, 12회 후 자동 페이드아웃. 화면 꺼짐 방지(Wake Lock)로 소리가 끊기지 않아요.
- **아주 작은 한 걸음** — 부담 없는 미션 추천.
- **🆘 많이 힘들 땐** — 위기 상담 전화(109·1577-0199·119)를 **항상** 볼 수 있는 카드. 번호를 누르면 바로 연결돼요.
- **잔잔한 배경음** — 🌧️ 빗소리 / 🌊 파도 / 🌲 숲·새소리 / 🔥 모닥불.
  - 음원 파일 없이 **Web Audio로 실시간 합성**: 빗방울·장작 탁탁임·새 지저귐 같은 디테일을
    무작위로 만들고, 잔향(리버브)으로 공간감을 더했어요. 오프라인에서도 그대로 동작.

### 🎯 챌린지 (90일 습관 만들기 · 복수 등록)
- 3달이면 습관이 된다는 점에 착안한 **90일 습관 트래커**. **여러 습관을 동시에** 등록·관리.
- 카드 한 장당 1탭으로 오늘 완료 체크, 펼치면 **90칸 잔디 그리드**.
- **실행의도(Implementation Intentions, Gollwitzer)**: "나는 [언제]에 [습관]을 한다" 문장으로 설계.
- **습관 쌓기(Habit Stacking, Fogg)**: 기존 행동(기상 후·양치 후 등)에 새 습관을 붙이는 트리거 선택.
- **2분 규칙 최소 버전**: 컨디션 낮은 날을 위한 작은 대안 기록.
- **비처벌 설계(Lally 2010)**: 빠진 날을 빨간 X가 아니라 회색 칸으로 — 한두 번 쉬어도 습관은 사라지지 않아요.
- **마일스톤 축하** — 1·3·7·21·30·66·90일 도달 시 메시지 + 사운드 + 컨페티 🎉
- 🔌 **캘린더(.ics) 연동** — 습관별로 구글/애플 캘린더에 90일치 매일 알림 추가.
- 🔌 **공유** — Web Share API로 진행 상황 공유 (미지원 시 클립보드 복사).

## 🧠 심리학 근거 (전문 자문 반영)

이 앱의 위로 문구·인사이트·습관 설계는 검증된 심리학 연구에 기반합니다.

- **자기자비(Self-Compassion)** — Neff (2003): 자기비판 대신 따뜻한 타당화 톤.
- **행동활성화(Behavioral Activation)** — Martell et al. (2010): "기분이 나아지길 기다리기보다 작은 행동 먼저".
- **습관형성** — Lally et al. (2010): 자동화 평균 66일·범위 18–254일, 1회 누락은 무영향 → 비처벌 설계.
- **실행의도** — Gollwitzer (1999): if-then 계획이 실천율을 높임.
- **정서 명명(Affect Labeling)** — Lieberman et al. (2007): 감정에 이름 붙이기가 편도체 활동을 낮춤 → 일기/기분 태깅.
- **자기결정이론(SDT)** — Deci & Ryan: 자율성·유능감·관계성. 강요·죄책감 대신 스스로 선택.
- **감사** — Emmons & McCullough (2003): 구체적 감사 3가지의 안녕감 효과.

**기록 탭 인사이트**는 위 이론을 if-then 규칙으로 구현했어요. 예: 최근 3일 기분이 낮으면 행동활성화를 권하고,
일기에서 위기 신호가 감지되면 **안전 카드**(상담 전화)를 최우선으로 띄웁니다.

> ⚠️ 이 앱은 셀프케어 보조 도구이며 진단·치료를 대신하지 않아요. 위기 시 자살예방상담 **109**(24시간).

## 🧪 시뮬레이션 베타테스트 & 개선

> ⚠️ **합성 데이터**: 실제 사용자가 아니라, 문헌·UX 휴리스틱에 기반해 생성한 **가상 페르소나 200명**을
> **90일**간 모델링한 결과입니다. 실제 사용자 검증으로 보완해야 합니다. (`node beta/simulate.mjs`, seed 고정 → 재현 가능)

**핵심 지표 (시뮬레이션):** 온보딩 81% · D1 90% → D7 61% → D30 28% → D90 6% · 만족도 2.98/5 · NPS −61 · 90일 완주 1%

**상위 개선 요구 → 이번에 구현한 것:**
| 베타 요구 | 구현 |
|---|---|
| 주간 회고 리포트 (#1) | 📊 **주간 리포트 카드** + 이미지 저장/공유 (기록 탭) |
| 지난 날 기록·수정 (#2) | 📅 **백필** — 날짜를 골라 과거 기록 추가/수정 (오늘 탭) |
| 자동 다크모드 (#3) | 🔄 **자동(시스템) 테마** |
| 어디서든 호흡 (#5) | 🌬️ **빠른 호흡 플로팅 버튼** + 전체화면 가이드 |
| 배경음 타이머 | ⏲️ **꺼짐 타이머** (15/30/60분) |
| 위로 톤이 과함 (#6) | 💬 **담백 모드** (다정/담백 톤 전환) + 위로 문구 확장 |
| 글씨가 작음 (고령층) | 🔠 **글자 크기** (보통/크게/아주 크게) |

**로드맵 (백엔드/플랫폼 필요 — 구조 변경):** 클라우드 백업·동기화, 진짜 푸시 알림, 함께하는 커뮤니티 챌린지, 홈 위젯, 습관별 개별 알림 시간.

리포트 전문: [`beta/REPORT.md`](beta/REPORT.md) · 원자료: `beta/personas.json`, `beta/results.json`

### 📊 기록 (데이터로 나 이해하기)
- **연속·전체·최근 기분** 요약.
- **🎯 습관 한눈에 (요약 서브탭)** — 습관 진행률·연속을 모아 보고 동그라미로 오늘 완료를 바로 체크해요. **습관 분석 인사이트 한 줄**도 함께. 4개 이상이면 '오늘 안 한 습관' 우선 3개 + 더보기로 한 화면 유지.
- **이번 주 한눈에** 카드에 **습관 달성률**을 핵심 지표로 추가해 한 주 데이터를 더 입체적으로.
- **마음 흐름·분포** — 최근 30일 점수 흐름 그래프와 기분 분포를 한 카드로.
- **주간/월간 리포트** — 요약 + 카드 이미지 저장/공유. **◀ 지난 주/달 이동**으로 과거 기간도 되짚어 보기.
- **🎯 습관 분석 (리포트)** — 요약 리포트에 습관별 달성률·**지난 기간 대비 증감**·현재 연속(🔥)·후반 **모멘텀(오름세/주춤)**·가장 잘 지키는 요일·기분 연관을 한 카드로 정리하고, 가장 꾸준한·살아나는·기분과 이어진 습관을 결론 문장으로 짚어줘요. (리포트 이미지에도 포함)
- **🏅 배지 & 레벨** — 기록·꾸준함·습관·마음챙김 등 7개 카테고리 51종 + 레벨. 기록 탭의 '배지' 서브탭에서 한눈에.
- **자동 인사이트** — "지난주보다 나아지고 있어요", "○요일에 유독 힘이 빠져요" 등 패턴 분석.
- **🔗 습관↔기분 상관관계** — "산책한 날 기분이 평균 0.6점 더 높아요"처럼 자기 이해를 도와요.
  표본 수(n)와 **효과크기(Cohen's d)** 를 함께 표기하고, *관찰적 상관(≠인과)* 임을 명시해요. (Cohen 1988; Lally 2010; Mazzucchelli 2010)
- **주간/월간 리포트 상세 페이지** — 날짜별 기록·분포·평균을 페이지로 펼쳐 보고 이미지/공유.
- **지난 기록 열람·수정·삭제** — 달력에서 날짜를 누르면 그날 기록을 한눈에 보고 수정/삭제할 수 있어요.

### ⚙️ 설정
- **테마** — 따뜻 🌅 / 차분 🌿 / 다크 🌙 / 보타니칼 🍃 / 나이트 🌌 / 자동 🔄
- **소리** — 효과음·호흡 가이드음 on/off
- **매일 기록 알림** — 원하는 시간에 부드럽게 리마인드 (앱이 열려 있을 때 동작)
- **데이터** — JSON 백업 내보내기 / 전체 삭제

## 사용 방법

`index.html` 을 브라우저로 열면 끝이에요. 또는 로컬 서버로:

```bash
python3 -m http.server 8000
# http://localhost:8000 접속
```

휴대폰 브라우저에서 "홈 화면에 추가"를 누르면 앱처럼 쓸 수 있어요.

## 문서
- 📚 **[docs/SCIENCE.md](docs/SCIENCE.md)** — 각 기능의 과학적 근거(논문·이유) 데이터베이스
- 📱 **[docs/RELEASE.md](docs/RELEASE.md)** — 앱 출시 가이드(iOS App Store · Android Google Play, Capacitor)

## 개발 / 테스트

```bash
npm install      # jsdom (테스트용)
npm test         # 핵심 사용자 흐름 회귀 스모크 테스트 (tests/smoke.mjs)
npm run beta     # 시뮬레이션 베타테스트 리포트 생성 (beta/)

# 스크롤 제로 레이아웃 검사 (요구: playwright-core + 크로미움, 로컬 서버)
python3 -m http.server 8787 &          # 앱 정적 서빙
node tools/fitcheck.mjs [chromium경로]  # 모든 탭·서브탭·오버레이가 한 화면(844/667px)에 들어가는지 실측
```

## ☁️ 클라우드 동기화 & 로그인 (선택)

설정하면 **여러 기기에서 같은 계정으로 로그인**해 기록이 자동 동기화돼요. 설정 전에는 기존처럼 이 기기에만 저장됩니다(완전 동작).

백엔드는 무료로 시작 가능한 **Supabase**를 사용합니다.

**1) 프로젝트 만들기**
[supabase.com](https://supabase.com) 가입 → New project 생성.

**2) 키 넣기**
Project Settings → **API** 에서 `Project URL` 과 `anon public` 키를 복사해 `config.js`에 붙여넣기:
```js
window.ONEUL_CONFIG = {
  SUPABASE_URL: "https://xxxx.supabase.co",
  SUPABASE_ANON_KEY: "eyJhbGci...",
};
```
> `anon` 키는 공개돼도 안전한 공개 키예요. 아래 RLS 정책으로 **본인 데이터만** 접근됩니다.

**3) 테이블 + 보안정책 (SQL Editor에 한 번 실행)**
```sql
create table if not exists public.app_state (
  user_id uuid references auth.users on delete cascade primary key,
  data jsonb not null default '{}',
  updated_at timestamptz default now()
);
alter table public.app_state enable row level security;
create policy "own state" on public.app_state
  for all using (auth.uid() = user_id) with check (auth.uid() = user_id);

-- 함께 탭: 공개 방 목록 — 만들면 모두에게 보임. 읽기는 누구나, 만들기·수정·삭제는 로그인한 본인 방만.
create table if not exists public.rooms (
  id text primary key,
  name text not null check (char_length(name) between 1 and 14),
  mood text default '' check (char_length(mood) <= 24),
  pat jsonb not null,
  owner uuid not null references auth.users(id) on delete cascade,
  owner_name text default '' check (char_length(owner_name) <= 12),
  created_at timestamptz default now()
);
alter table public.rooms enable row level security;
create policy "rooms readable by everyone" on public.rooms for select using (true);
create policy "insert own rooms" on public.rooms for insert with check (auth.uid() = owner);
create policy "update own rooms" on public.rooms for update using (auth.uid() = owner) with check (auth.uid() = owner);
create policy "delete own rooms" on public.rooms for delete using (auth.uid() = owner);
```

**4) 로그인 방식**
- **이메일/비밀번호**: 기본 활성화(Authentication → Providers → Email). 바로 회원가입/로그인 가능.
- **Google 로그인(선택)**: Authentication → Providers → Google 활성화 + Google Cloud OAuth 클라이언트 설정. 설정 전엔 "Google로 계속" 버튼이 동작하지 않아요.

**5) 사용**
설정 탭 → ☁️ 계정 & 동기화 에서 로그인. 이후 기록 변경 시 자동 저장되고, 다른 기기에서 로그인하면 **병합**돼요(일기는 수정 시각 기준 최신 우선, 습관 완료는 **날짜별 토글 시각 기준 최신** — 한쪽의 '완료 해제'가 다른 기기의 옛 완료에 덮이지 않아요).

> 동작 방식: 변경 시 원격을 먼저 받아 **병합 후 업로드**(blind overwrite 방지)해서 다른 기기의 편집이 사라지지 않아요. **삭제는 묘비(tombstone)로 전파**돼 지운 기록이 다른 기기에서 되살아나지 않습니다. 실시간 양방향 푸시는 아니므로, 다른 기기 변경은 그 기기에서 "지금 동기화"나 재로그인 시 반영돼요.

## 개인정보

기본적으로 모든 기록은 **사용자 기기(localStorage)에만** 저장돼요. 클라우드 동기화를 **직접 설정해 로그인한 경우에만**, 본인 계정의 Supabase에 암호화 전송되어 저장됩니다(RLS로 본인만 접근). 동기화를 켜지 않으면 외부 전송이 전혀 없어요.

## 마음이 너무 힘든 날엔

혼자 견디지 말아요. 도움을 청하는 건 용기예요.

- 정신건강 상담전화 **1577-0199** (24시간)
- 자살예방 상담전화 **109** (24시간)
